{% extends 'FlowcodeDashboardBundle:Default:layout.html.twig' %}

{% block body -%}
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            {{ 'gallery.list.title'|trans }}
        </h1>
        <ol class="breadcrumb">
            <li><a href="{{ path('admin_dashboard') }}"><i class="fa fa-dashboard"></i> {{ 'home'|trans|capitalize }}</a></li>
            <li class="active">Gallery</li>

        </ol>
    </section>

    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">All galleries</h3>
                <div class="box-tools">
                    <ul class="list-inline" style="display: inline-block;">
                        <li>
                            <a href="{{ path('admin_gallery_new') }}" class="btn btn-primary btn-sm">
                                <i class="fa fa-plus"></i> New Gallery
                            </a>
                        </li>
                    </ul>
                    <form action="{{ path('admin_gallery') }}" method="get" class="form-inline" role="form" style="display: inline-block;">
                        <input class="form-control caja-buscar" placeholder="Buscar por nombre" type="text" name="search" value="{{ searchStr }}">
                        <select class="form-control" name="type">
                            <option value="">Tipo de galería</option>
                            <option value="gallery_slider">Slider</option>
                            <option value="gallery_product">Producto</option>
                        </select>
                        <button class="btn btn-default btn-sm" type="submit">
                            <i class="fa fa-search"></i> Buscar
                        </button>
                    </form>
                </div>
            </div>
            <div class="box-body">            
                <table class="table table-condensed">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Slug</th>
                            <th>Enabled</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for entity in pagination %}
                            <tr>
                                <td><a href="{{ path('admin_gallery_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                                <td>{{ entity.name }}</td>
                                <td>{{ entity.slug }}</td>
                                <td>{{ entity.enabled }}</td>
                                <td>
                                    <ul class="list-inline">
                                        <li>
                                            <a href="{{ path('admin_gallery_items', { 'id': entity.id }) }}">items</a>
                                        </li>
                                        <li>
                                            <a href="{{ path('admin_gallery_show', { 'id': entity.id }) }}">show</a>
                                        </li>
                                        <li>
                                            <a href="{{ path('admin_gallery_edit', { 'id': entity.id }) }}">edit</a>
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
                <div class="navigation">
                    {{ knp_pagination_render(pagination) }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
